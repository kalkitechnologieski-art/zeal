{
  "name": "@zeal/database",
  "version": "0.0.1",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "dependencies": {
    "@prisma/client": "^5.15.0"
  },
  "devDependencies": {
    "@prisma/config": "7.9.1",
    "prisma": "^5.15.0",
    "typescript": "^5.5.0"
  },
  "scripts": {
    "generate": "prisma generate --schema=prisma/schema.prisma",
    "migrate": "prisma migrate dev",
    "studio": "prisma studio",
    "type-check": "tsc --noEmit",
    "prisma:generate": "npx prisma generate --schema=./prisma/schema.prisma --config=./prisma.config.js"
  }
}