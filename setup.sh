# Add all changes
git add .

# Commit with a descriptive message
git commit -m "feat: Production-ready rewrite with AI female astrologers, wallet, and Supabase integration"

# Push to remote (assuming origin and branch master)
git push origin master